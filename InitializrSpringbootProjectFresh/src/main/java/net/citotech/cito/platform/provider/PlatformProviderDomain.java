package net.citotech.cito.platform.provider;

/** Domains that can bind to an external provider through Cito's common provider fabric. */
public enum PlatformProviderDomain {
    PAYMENT,
    COMMUNICATION,
    IDENTITY,
    RISK,
    VENDING,
    REGULATORY
}
